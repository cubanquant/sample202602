# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Sample repository for IS211 (Spring 2026). Contains Python scripts for course assignments.

## Running Scripts

```bash
# hello_world.py - no arguments needed
python hello_world.py

# assignment2.py - requires a --url argument pointing to a data file
python assignment2.py --url <datafile_url>
```

## Language & Dependencies

- Python 3 (standard library only: `argparse`, `urllib.request`, `logging`, `datetime`)
- No external dependencies or package manager required
